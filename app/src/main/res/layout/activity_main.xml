<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <com.demo.it.circleloading.LoadingCircleView
        android:id="@+id/loading_circle"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        app:circle_background="@color/color_bg"
        app:circle_loading="@color/color_loading" />

    <com.demo.it.circleloading.ArcLoadingView
        android:id="@+id/loading_arc"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_below="@id/loading_circle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp" />

    <com.demo.it.circleloading.LineLoadingView
        android:id="@+id/line_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/loading_arc"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="10dp" />
</RelativeLayout>
